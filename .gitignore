# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri / desktop build output (compiled binaries + installers live here).
# Also covered by src-tauri/.gitignore; duplicated at root as a safety net.
/src-tauri/target
/src-tauri/gen/schemas

# Playwright (screenshot generation) — keep the PNGs in docs/, drop the run artifacts.
/test-results
/playwright-report
/blob-report
/playwright/.cache
